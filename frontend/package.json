{
    "name": "expense-viewer-frontend-workspace",
    "private": true,
    "packageManager": "pnpm@11.20.0",
    "scripts": {
        "dev": "pnpm --filter charts-remote build && pnpm --filter dashboard build && pnpm --filter import build && concurrently -n remote,dashboard,import,host -c blue,magenta,cyan,green \"pnpm --filter charts-remote preview\" \"pnpm --filter dashboard preview\" \"pnpm --filter import preview\" \"pnpm --filter host dev\"",
        "build:deploy": "./build-for-deploy.sh",
        "lint": "oxlint .",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "devDependencies": {
        "concurrently": "^9.1.0",
        "oxlint": "^1.71.0",
        "prettier": "^3.9.4"
    }
}
